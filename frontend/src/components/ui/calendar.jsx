// Calendar component stubbed — react-day-picker removed (unused in this app).
// react-day-picker@8 is incompatible with React 19.
// To add a date picker in the future, use react-day-picker@9+ which supports React 19.
export function Calendar() {
  return null;
}
